# Type Name  ID  GECOS          Home            Shell
u cameo      -   "Cameo daemon" /var/lib/cameo /usr/bin/nologin
# /dev/kfd and /dev/dri/renderD* are root:render 0660 (udev); the daemon's
# llama-server children need both or every backend silently falls back to CPU.
m cameo render
m cameo video
