#!/usr/bin/env bash
# Signed offline appliance update entry point. The transaction implementation is
# separate so it can be tested without changing this stable command path.
set -euo pipefail
exec /usr/local/lib/cameo/update-transaction "$@"
