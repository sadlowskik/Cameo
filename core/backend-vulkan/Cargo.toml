[package]
name = "cameo-backend-vulkan"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "llama.cpp Vulkan backend — the universal baseline (thin executor over the placement command boundary)."

[dependencies]
cameo-placement.workspace = true
